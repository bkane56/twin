## This is a project for a class I was doing.  It creates a AI digital twin of me.
### It uses AI with RAG data from LinkedIn as well as files I added
### It also uses Terraform to push the code to AWS and has a Git action hook to rebuild and publish on commits to master.
